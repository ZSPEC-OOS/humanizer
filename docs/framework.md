# Framework

Humanizer Lab combines deterministic rule learning with optional AI rewriting.

1. Analyze paired examples (AI text + humanized text)
2. Extract explainable rules with evidence
3. Merge rules into a managed rule bank
4. Convert with rule-only, AI-only, or hybrid mode
5. Score outcomes with heuristics and optional model scoring

This architecture keeps the system auditable and extensible.
